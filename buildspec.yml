version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 10

  build:
    commands:
      - echo Nothing to do yet

artifacts:
  files:
    - images/*
    - styles/main.css
    - favicon.ico
    - index.html
    - upload-portfolio-lambda.py
