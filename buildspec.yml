version: 0.2

run-as: Linux-user-name

phases:
  build:
    commands:
      - echo nothing to do yet

  install:
    run-as: Linux-user-name
    runtime-versions:
      nodejs: 10

artifacts:
  files:
    - images/*
    - styles/main.class
    - favicon.icon
    - index.html
